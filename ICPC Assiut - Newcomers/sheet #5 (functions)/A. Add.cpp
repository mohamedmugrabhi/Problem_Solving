#include <bits/stdc++.h>
using namespace std;

void fast(){
  ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
}

int sum(int x, int y) {
    return x + y;
}

int main() {
  fast();
  
 int x, y;
 cin >> x >> y;
 cout << sum(x, y);
  
  return 0;  
}
